// @flow
import styled from 'styled-components';

const NotificationDotBadge = styled.span`
  width: 0.6rem;
  height: 0.6rem;
  border-radius: 50%;
  background: #ee3c25;
  float: left;
  margin: 0.9rem 0.4rem 0.9rem 0;
`;

export default NotificationDotBadge;
